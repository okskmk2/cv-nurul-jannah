---
type Props = { class?: string; decorative?: boolean };
const { class: className = "h-10 w-10", decorative = true } = Astro.props;
---
<img
  src="/logo.png"
  alt={decorative ? "" : "CV. Nurul Jannah"}
  width={512}
  height={512}
  class:list={["object-contain", className]}
/>
