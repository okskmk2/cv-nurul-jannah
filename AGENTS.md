# CV. Nurul Jannah — Astro

Static Astro site with React islands. Pages live in `src/pages`. Interactive UI is only:

- `src/components/react/Header.tsx`
- `src/components/react/LanguageSwitcher.tsx`
- `src/components/react/ContactForm.tsx`

Do not add `"use client"` page trees. Keep public URLs, hreflang, and Cloudflare `EMAIL` binding.
